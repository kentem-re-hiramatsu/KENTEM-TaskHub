import type { Meta, StoryObj } from '@storybook/react';
import { {{ pascalCase name }} } from './{{ kebabCase name }}';

const meta = {
  {{#if (eq kind 'components')}}
  title: '{{ camelCase kind }}/{{ pascalCase name }}',
  {{ else if (eq feature '新規作成') }}
  title: '{{ camelCase kind }}/{{ camelCase newFeature }}/{{ pascalCase name }}',
  {{ else }}
  title: '{{ camelCase kind }}/{{ camelCase feature }}/{{ pascalCase name }}',
  {{/if}}
  component: {{ pascalCase name }},
  args: {},
} satisfies Meta<typeof {{ pascalCase name }}>;

export default meta;

export const Default: StoryObj<typeof {{ pascalCase name }}> = {};