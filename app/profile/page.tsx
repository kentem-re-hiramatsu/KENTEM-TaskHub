import { PersonalSettingsContainer } from '@/features/settings/personal/components/personal-settings-container';

const Page = async () => {
  return <PersonalSettingsContainer />;
};

export default Page;
